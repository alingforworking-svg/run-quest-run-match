"use client";
import { useRouter } from "next/navigation";
import { Footprints,MapPin,Radar,Users } from "lucide-react";
import { useGame } from "@/components/state/game-provider";
export function QuestActions({questId,venueName}:{questId:string;venueName:string}){const router=useRouter(),{startQuest}=useGame();function choose(path:string){startQuest(questId);router.push(path)}return <div className="mt-7"><p className="mb-2 text-[.52rem] font-black tracking-widest text-[#85899b]">CHOOSE HOW TO COMPLETE THIS QUEST</p><div className="grid grid-cols-2 gap-2"><button onClick={()=>choose(`/run/${questId}?mode=location`)} className="flex min-h-20 flex-col items-center justify-center rounded-2xl bg-[#b7ff22] px-2 text-center text-[#171720]"><MapPin size={19}/><b className="mt-2 text-[.62rem]">RUN AT LOCATION</b><small className="mt-1 max-w-full truncate text-[.45rem] font-black opacity-65">{venueName}</small></button><button onClick={()=>choose(`/run/${questId}?mode=anywhere`)} className="flex min-h-20 flex-col items-center justify-center rounded-2xl border border-[#59e0dc] bg-[#203239] px-2 text-center text-white"><Footprints size={19} className="text-[#59e0dc]"/><b className="mt-2 text-[.62rem]">RUN ANYWHERE</b><small className="mt-1 text-[.45rem] font-black text-[#9dbcc1]">DISTANCE ONLY</small></button></div><div className="mt-2 grid grid-cols-2 gap-2"><button onClick={()=>choose(`/match?quest=${questId}`)} className="flex min-h-11 items-center justify-center gap-2 rounded-xl bg-[#7439ee] px-2 text-[.55rem] font-black text-white"><Users size={15}/>FIND RUNNER</button><button onClick={()=>choose(`/run-signal?quest=${questId}`)} className="flex min-h-11 items-center justify-center gap-2 rounded-xl bg-[#59e0dc] px-2 text-[.55rem] font-black text-[#171720]"><Radar size={15}/>RUN SIGNAL</button></div></div>}
