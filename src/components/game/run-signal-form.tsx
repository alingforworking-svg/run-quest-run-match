"use client";
import { useState } from "react";
import Link from "next/link";
import { Clock3,MapPin,Radar,Users,Zap } from "lucide-react";
import { useGame } from "@/components/state/game-provider";
import { useLiveRunners } from "@/hooks/use-live-runners";
export function RunSignalForm(){const {state,setRunSignal}=useGame(),{runners,loading}=useLiveRunners(true),saved=state.runSignal;const [active,setActive]=useState(Boolean(saved?.active)),[distance,setDistance]=useState(saved?.distance??5),[radius,setRadius]=useState(saved?.radius??3),[pace,setPace]=useState(saved?.pace??"6:00–7:00 / KM"),[duration,setDuration]=useState(saved?.duration??60),[when,setWhen]=useState("NOW"),[party,setParty]=useState("1 PARTNER");function send(){setRunSignal({active:true,distance,radius,pace,duration});setActive(true)}function edit(){setRunSignal(null);setActive(false)}if(active)return <div className="pop"><div className="relative mx-auto grid size-28 place-items-center rounded-full bg-[#b7ff22] text-[#111]"><Radar size={43}/><i className="absolute inset-0 animate-ping rounded-full border border-[#b7ff22]"/></div><p className="eyebrow mt-10 text-center">SIGNAL ACTIVE • {duration}:00 LEFT</p><h2 className="game-title mt-3 text-center text-4xl">{loading?"…":runners.length} REAL RUNNERS<br/><span className="text-[#b7ff22]">READY NOW.</span></h2>{runners.length?<div className="mt-8 grid gap-3 sm:grid-cols-2">{runners.slice(0,4).map(r=><Link href={`/profile/${r.username}`} key={r.id} className="panel lift flex items-center gap-3 p-3 text-left"><span className="grid size-12 place-items-center rounded-full bg-[#9677dc] text-3xl">{r.avatar}</span><span className="flex-1"><b className="block text-xs">{r.name.toUpperCase()}</b><small className="text-[.5rem] font-bold text-[#878b9e]">PACE {r.paceMinKm.toFixed(2).replace(".",":")} • LIVE SIGNAL</small></span><Zap size={16} className="text-[#b7ff22]"/></Link>)}</div>:!loading&&<p className="muted mt-5 text-center text-xs">No other real runner has an active signal yet.</p>}<div className="mt-5 flex justify-center"><button onClick={edit} className="ghost-button">STOP / EDIT RUN SIGNAL</button></div></div>;return <div><div className="grid gap-4 sm:grid-cols-2"><Choice title="WHEN" icon={<Clock3/>} options={["NOW","LATER"]} value={when} setValue={setWhen}/><Choice title="PARTY TYPE" icon={<Users/>} options={["1 PARTNER","GROUP"]} value={party} setValue={setParty}/></div><div className="mt-4 grid gap-4 sm:grid-cols-2"><Range label="DISTANCE" value={distance} setValue={setDistance} min={2} max={15}/><Range label="SEARCH RADIUS" value={radius} setValue={setRadius} min={1} max={10}/></div><div className="panel mt-4 grid gap-4 p-5 sm:grid-cols-2"><label><span className="text-[.55rem] font-black text-[#83879a]">PREFERRED PACE</span><select value={pace} onChange={e=>setPace(e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-[#35384d] bg-[#1c1e31] px-3 text-xs font-black"><option>6:00–7:00 / KM</option><option>5:00–6:00 / KM</option><option>7:00–8:00 / KM</option></select></label><label><span className="text-[.55rem] font-black text-[#83879a]">AVAILABLE DURATION</span><select value={duration} onChange={e=>setDuration(Number(e.target.value))} className="mt-2 h-12 w-full rounded-xl border border-[#35384d] bg-[#1c1e31] px-3 text-xs font-black"><option value="60">60 MINUTES</option><option value="30">30 MINUTES</option><option value="90">90 MINUTES</option></select></label></div><button onClick={send} className="lime-button mt-5 w-full"><Radar size={18}/>SEND LIVE RUN SIGNAL</button><p className="mt-4 flex items-center justify-center gap-2 text-[.52rem] text-[#777b8e]"><MapPin size={12}/>Only your preferences are shared. Exact live movement is never public.</p></div>}
function Choice({title,icon,options,value,setValue}:{title:string;icon:React.ReactNode;options:string[];value:string;setValue:(v:string)=>void}){return <div className="panel p-5"><div className="flex items-center gap-2 text-[#b7ff22]">{icon}<span className="text-[.55rem] font-black text-[#898da0]">{title}</span></div><div className="mt-4 grid grid-cols-2 gap-2">{options.map(o=><button onClick={()=>setValue(o)} key={o} className={`h-11 rounded-xl border text-[.58rem] font-black ${value===o?"border-[#b7ff22] bg-[#b7ff22] text-[#111]":"border-[#34374c] bg-[#1b1d2f]"}`}>{o}</button>)}</div></div>}
function Range({label,value,setValue,min,max}:{label:string;value:number;setValue:(n:number)=>void;min:number;max:number}){return <div className="panel p-5"><div className="flex justify-between"><span className="text-[.55rem] font-black text-[#898da0]">{label}</span><b className="text-[#b7ff22]">{value} KM</b></div><input aria-label={label} type="range" min={min} max={max} value={value} onChange={e=>setValue(Number(e.target.value))} className="mt-5 w-full accent-[#b7ff22]"/></div>}
