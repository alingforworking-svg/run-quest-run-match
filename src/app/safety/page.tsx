"use client";
import { useState } from "react";
import { AppShell } from "@/components/layout/app-shell";
import { AlertTriangle,Ban,MapPin,Phone,ShieldCheck,UserRound,X } from "lucide-react";
import { useGame } from "@/components/state/game-provider";
const tools=[[MapPin,"PUBLIC MEETING POINTS","Parties meet at verified parks and landmarks, never home addresses."],[Phone,"EMERGENCY CONTACT","Add a trusted contact to your active run screen."],[UserRound,"SHARE RUN","Share a time-limited live run link with someone you trust."],[Ban,"BLOCK & REPORT","Immediately block a runner or report unsafe behavior."]] as const;
export default function Safety(){const [open,setOpen]=useState(false),[category,setCategory]=useState("Unsafe behavior"),[detail,setDetail]=useState("");const {addReport}=useGame();function submit(e:React.FormEvent){e.preventDefault();addReport(category,detail);setOpen(false);setDetail("")}return <AppShell title="SAFETY"><main className="mx-auto max-w-3xl px-4 py-8 md:px-8"><div className="rounded-[1.8rem] border border-[#3e5940] bg-gradient-to-br from-[#1b321f] to-[#141d19] p-7 text-center"><ShieldCheck className="mx-auto text-[#b7ff22]" size={42}/><p className="eyebrow mt-5">RUN WITH CONFIDENCE</p><h1 className="game-title mt-2 text-4xl md:text-5xl">SAFETY IS PART<br/>OF THE QUEST.</h1><p className="muted mx-auto mt-4 max-w-lg text-xs leading-5">RUN QUEST never shows another runner&apos;s precise live location or historical home/start points publicly.</p></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{tools.map(([Icon,title,text])=><article key={title} className="panel p-5"><span className="grid size-11 place-items-center rounded-xl bg-[#222539] text-[#b7ff22]"><Icon size={20}/></span><h3 className="mt-4 text-xs font-black">{title}</h3><p className="muted mt-2 text-[.62rem] leading-5">{text}</p></article>)}</div><button onClick={()=>setOpen(true)} className="mt-5 flex w-full items-center justify-center gap-2 rounded-2xl border border-[#603638] bg-[#2c1d24] py-4 text-[.65rem] font-black text-[#ff8570]"><AlertTriangle size={17}/>REPORT A SAFETY ISSUE</button>{open&&<div className="fixed inset-0 z-[100] grid place-items-center bg-black/75 p-4"><form onSubmit={submit} className="panel w-full max-w-md p-5"><div className="flex justify-between"><h2 className="game-title text-3xl">SAFETY REPORT</h2><button type="button" onClick={()=>setOpen(false)} className="icon-button"><X/></button></div><select value={category} onChange={e=>setCategory(e.target.value)} className="mt-5 h-12 w-full rounded-xl border border-[#35384d] bg-[#1c1e31] px-3 text-xs"><option>Unsafe behavior</option><option>Harassment</option><option>Fake GPS activity</option><option>Emergency</option></select><textarea required minLength={8} value={detail} onChange={e=>setDetail(e.target.value)} placeholder="Describe what happened…" className="mt-3 min-h-32 w-full rounded-xl border border-[#35384d] bg-[#1c1e31] p-3 text-xs outline-none"/><button className="lime-button mt-3 w-full">SUBMIT REPORT</button></form></div>}</main></AppShell>}
