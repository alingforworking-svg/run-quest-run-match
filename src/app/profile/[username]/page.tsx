import { AppShell } from "@/components/layout/app-shell";import { PlayerProfile } from "@/components/profile/player-profile";
export default async function PublicProfile({params}:{params:Promise<{username:string}>}){const {username}=await params;return <AppShell title="RUNNER PROFILE"><main className="mx-auto max-w-5xl px-4 py-8 md:px-8"><PlayerProfile publicView username={username}/></main></AppShell>}
