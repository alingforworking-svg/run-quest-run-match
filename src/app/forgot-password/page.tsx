"use client";import { useState } from "react";import Link from "next/link";import { ArrowLeft,Check,Mail } from "lucide-react";import { Logo } from "@/components/ui/logo";import { createClient,isSupabaseConfigured } from "@/lib/supabase/client";
export default function ForgotPassword(){const [sent,setSent]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const email=String(new FormData(e.currentTarget).get("email"));const client=createClient();if(client)await client.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/login`});setSent(true)}return <main className="game-bg min-h-screen p-5"><Logo href="/"/><div className="grid min-h-[calc(100vh-70px)] place-items-center"><div className="w-full max-w-md text-center">{sent?<><span className="mx-auto grid size-16 place-items-center rounded-2xl bg-[#b7ff22] text-[#111]"><Check/></span><h1 className="game-title mt-5 text-4xl">CHECK YOUR EMAIL.</h1><p className="muted mt-3 text-xs">{isSupabaseConfigured?"We sent a secure password reset link.":"Password reset is unavailable until the live database is configured."}</p></>:<><p className="eyebrow">ACCOUNT RECOVERY</p><h1 className="game-title mt-3 text-4xl">RESET YOUR PASSWORD.</h1><form onSubmit={submit} className="panel mt-6 p-5 text-left"><label><span className="text-[.52rem] font-black tracking-widest text-[#85899b]">EMAIL</span><div className="mt-2 flex h-12 items-center rounded-xl border border-[#34374b] bg-[#1b1d2f] px-3"><Mail size={15}/><input required type="email" name="email" className="h-full flex-1 bg-transparent px-3 text-xs outline-none"/></div></label><button className="lime-button mt-4 w-full">SEND RESET LINK</button></form></>}<Link href="/login" className="mt-6 inline-flex items-center gap-2 text-[.58rem] font-black text-[#9296a8]"><ArrowLeft size={13}/>BACK TO LOGIN</Link></div></div></main>}
