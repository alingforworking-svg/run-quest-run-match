"use client";import { useCallback,useState } from "react";import type { Coordinates } from "@/types/game";
export function useGeolocation(){const [location,setLocation]=useState<Coordinates|null>(null);const [status,setStatus]=useState<"idle"|"loading"|"granted"|"denied"|"unsupported">("idle");const request=useCallback(()=>{if(!navigator.geolocation){setStatus("unsupported");return}setStatus("loading");navigator.geolocation.getCurrentPosition(p=>{setLocation({lat:p.coords.latitude,lng:p.coords.longitude});setStatus("granted")},()=>setStatus("denied"),{enableHighAccuracy:true,timeout:10000,maximumAge:30000})},[]);return{location,status,request}}
